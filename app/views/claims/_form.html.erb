
<div id="claim-modal" class="modal fade">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
        <h4 class="modal-title"><%= @poster?'Add response to claims':'Claim or Contact' %></h4>
      </div>
      <%= form_for [@post, @claim], remote: true do |f| %>
      <div class="modal-body">
        <% if @poster %>
          <%= f.hidden_field :name, :value => @post.name%>
          <%= f.hidden_field :email, :value => @post.email%>
        <% else %>
          <div class="form-group">
            <p><i>Unless marked private, your name and note will show up on the post page.</i></p>
            <%= f.text_field :name, class: 'form-control input-lg', placeholder: "Name" %>
          </div>
          <div class="form-group">
            <%= f.text_field :email, class: 'form-control input-lg', placeholder: "E-Mail" %>
            <p><i>Your e-mail will <em>not</em> be displayed.</i></p>
            <p>If you have a <a href="http://www.gravatar.com">gravatar</a> image, it will be used.</p>
          </div>
        <% end %>
        <div class="form-group">
          <%= f.text_area :note, class: 'form-control input-lg', placeholder: "Note" %>
          <% if !@poster %>
            <%= f.label :private, "Private" %>
            <%= f.check_box :private, 'data-toggle'=>'toggle', label: "Private" %>
            <p><i>If selected this will not show up on post page but will still be sent to the person who made the post.</i></p>
          <% end %>
        </div>
        
        
                <p><small><i>Disclaimer:</i> Lovefund.us is intended to help match people who want to help others with people who could use the help.  This site is not vetted.  The Church in Drive does not have the resources to verify every post.  Use at your own risk.  Please meet in public places and do not give out personal information until you are comfortable doing so and confident that it is safe.</small></p>
      </div>
      <div class="modal-footer text-center">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        <button type="submit" name="commit" value="Submit" class="btn btn-primary">Submit</button>
      </div>
      <% end %>
    </div>
  </div>
</div>