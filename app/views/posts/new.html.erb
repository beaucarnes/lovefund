<% provide(:title, 'New') %>
<h1><%= Post.categories.keys[params[:category].to_i].humanize.titleize %></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">

<%= render 'form' %>

<%= link_to 'Back', posts_path %>

  </div>
</div>
